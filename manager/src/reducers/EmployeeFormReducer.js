import {
  EMPLOYEE_UPDATE
} from '../actions/types';

const INITICIAL_STATE = {};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
}
